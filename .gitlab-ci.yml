before_script:
  - source /opt/miniconda/bin/activate

build:
  stage: build
  script:
    - cd $CI_COMMIT_REF_NAME
    - conda build . --prefix-length=80 --output-folder=../dist/ --python=2.7 --channel=file://conda/bcu
    # - conda build . --prefix-length=80 --output-folder=../dist/ --python=3.6 --channel=file://conda/bcu
  artifacts:
    paths:
      - dist/

deploy:
  stage: deploy
  dependencies:
    - build
  script:
    - mv -f dist/* /conda/linux-64/
    - conda index  /conda/linux-64/